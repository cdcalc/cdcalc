dependencies {
    compile gradleApi()
}

apply plugin: 'maven-publish'

publishing {
    publications {
        mavenJava(MavenPublication) {
            artifactId 'cdcalc-gradle'
            from components.java
        }
    }
}
